import { Element } from '.'

class SpaceElement extends Element {
    public readonly name = 'space'
    public readonly defaultParam = null
    public readonly params = {}
}

export default SpaceElement
